<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:tools="http://schemas.android.com/tools">

    <RelativeLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:padding="1dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

      <!--  <ProgressBar
            android:id="@+id/countdownProgressbar"
            style="?android:attr/progressBarStyleHorizontal"
            android:layout_width="@dimen/secondary_circle_width"
            android:layout_height="@dimen/secondary_circle_height"
            android:layout_centerInParent="true"
            android:background="@drawable/progressbar_circularshape"
            android:indeterminate="false"
            android:max="@{progressMaxValue}"
            android:progress="@{progressValue}"
            android:progressDrawable="@drawable/circular_progress_bar"
            tools:max="100"
            tools:progress="65" />-->

        <TextView
            android:id="@+id/contTimerText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:gravity="center"
            android:lineSpacingExtra="4dp"
            android:padding="4dp"
            android:text="@{progressText}"
            android:textColor="@color/black"
            android:textSize="10sp"
            android:textStyle="normal"
            tools:text="1 of 6" />

    </RelativeLayout>

</androidx.constraintlayout.widget.ConstraintLayout>